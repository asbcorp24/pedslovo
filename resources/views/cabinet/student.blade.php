@extends('layouts.app')
@section('title','Личный кабинет студента — Педслово')
@section('content')<div class="container py-5"><div class="row g-4"><div class="col-lg-3"><div class="card card-soft"><div class="card-body p-4 text-center"><div class="rounded-circle bg-wine text-white mx-auto d-grid mb-3" style="width:72px;height:72px;place-items:center;font-size:1.8rem">{{ mb_strtoupper(mb_substr($u->name,0,1)) }}</div><h1 class="h5">{{ $u->name }}</h1><div class="text-muted small">Студент</div><hr>@foreach($groups as $g)<span class="badge bg-light text-dark mb-1">{{ $g->name }}</span>@endforeach</div></div></div><div class="col-lg-9"><div class="d-flex justify-content-between align-items-end mb-3"><div><div class="eyebrow">Личный кабинет</div><h2 class="h2 mb-0">Моё обучение</h2></div><a class="btn btn-outline-dark" href="{{ route('certificates.index') }}">Сертификаты</a></div><div class="row g-3">@forelse($enrollments as $e)@php($s=$stats[$e->id])<div class="col-md-6"><div class="card card-soft h-100"><div class="card-body p-4"><div class="d-flex justify-content-between"><span class="badge bg-light text-dark">{{ $e->course->study_year ? $e->course->study_year.' курс' : 'Программа' }}</span><span class="small text-muted">{{ $s['percent'] }}%</span></div><h3 class="h5 mt-3">{{ $e->course->title }}</h3><div class="progress my-3"><div class="progress-bar bg-wine" style="width:{{ $s['percent'] }}%"></div></div><div class="small text-muted mb-3">Пройдено {{ $s['done'] }} из {{ $s['total'] }} уроков</div><a class="btn btn-wine btn-sm" href="{{ route('courses.show',$e->course) }}">Продолжить обучение</a></div></div></div>@empty<div class="col-12"><div class="alert alert-light border">Вы пока не записаны на учебные курсы.</div></div>@endforelse</div>@if($certificates->count())<h2 class="h4 mt-5">Последние сертификаты</h2><div class="list-group">@foreach($certificates as $c)<a class="list-group-item list-group-item-action d-flex justify-content-between" href="{{ route('certificates.show',$c) }}"><span>{{ $c->course->title }}</span><span class="text-muted">{{ optional($c->issued_at)->format('d.m.Y') }}</span></a>@endforeach</div>@endif</div></div></div>@endsection

@extends('layouts.app')
@section('title','Кабинет преподавателя — Педслово')
@section('content')<div class="container py-5"><div class="eyebrow">Кабинет преподавателя</div><div class="d-flex flex-wrap justify-content-between align-items-end mb-4"><div><h1 class="display-6 fw-bold">{{ $u->name }}</h1><p class="text-muted mb-0">Ваши дисциплины, группы и журналы успеваемости.</p></div></div><div class="row g-4"><div class="col-lg-8"><h2 class="h3">Мои учебные курсы</h2><div class="row g-3">@forelse($courses as $course)<div class="col-md-6"><div class="card card-soft h-100"><div class="card-body p-4"><span class="badge bg-light text-dark">{{ $course->study_year ? $course->study_year.' курс' : 'Программа' }}</span><h3 class="h5 mt-2">{{ $course->title }}</h3><div class="small text-muted mb-3">{{ optional($course->section)->title }} · студентов: {{ $course->enrollments_count }}</div><a href="{{ route('cabinet.teacher.journal',$course) }}" class="btn btn-wine btn-sm"><i class="bi bi-table"></i> Открыть журнал</a><a href="{{ route('courses.show',$course) }}" class="btn btn-outline-dark btn-sm">Курс</a></div></div></div>@empty<div class="col-12"><div class="alert alert-light border">За вами пока не закреплены учебные курсы.</div></div>@endforelse</div></div><div class="col-lg-4"><h2 class="h3">Мои группы</h2>@forelse($groups as $group)<div class="card card-soft mb-3"><div class="card-body"><h3 class="h6">{{ $group->name }}</h3><div class="small text-muted">Студентов: {{ $group->users_count }}</div><div class="small text-muted">Курсов: {{ $group->courses->count() }}</div></div></div>@empty<div class="alert alert-light border">Кураторские группы не назначены.</div>@endforelse</div></div></div>@endsection

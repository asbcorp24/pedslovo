@extends('admin.layout') @section('title','SCORM / iSpring') @section('content')<h1 class="mb-2">SCORM / iSpring</h1><p class="text-muted">Загрузите ZIP, опубликованный из iSpring в формате SCORM 1.2 или SCORM 2004. Пакет запускается прямо на сайте, а статус и балл сохраняются в Laravel.</p><div class="card border-0 shadow-sm mb-4"><div class="card-body p-4"><form method="post" enctype="multipart/form-data" action="{{ route('admin.scorm.store') }}">@csrf<div class="row g-3 align-items-end"><div class="col-md-5"><label class="form-label">Название теста/курса</label><input class="form-control" name="title" required></div><div class="col-md-5"><label class="form-label">SCORM ZIP</label><input class="form-control" type="file" name="package" accept=".zip" required></div><div class="col-md-2"><button class="btn btn-dark w-100">Загрузить</button></div></div></form></div></div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Название</th><th>Версия</th><th>Попыток</th><th></th></tr></thead><tbody>@forelse($packages as $p)<tr><td>{{ $p->title }}</td><td>{{ $p->version }}</td><td>{{ $p->attempts()->count() }}</td><td class="text-end"><a class="btn btn-sm btn-primary" href="{{ route('scorm.launch',$p) }}">Запустить</a><form method="post" action="{{ route('admin.scorm.destroy',$p) }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Удалить пакет и результаты?')">Удалить</button></form></td></tr>@empty<tr><td colspan="4" class="text-muted">SCORM-пакеты пока не загружены.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $packages->links() }}</div>@endsection

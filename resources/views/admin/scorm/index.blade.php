@extends('admin.layout') @section('title','SCORM / iSpring') @section('content')<h1>SCORM / iSpring</h1><p class="text-muted">Загрузите ZIP из iSpring. Можно ограничить число попыток и задать проходной балл.</p><div class="card border-0 shadow-sm mb-4"><div class="card-body"><form method="post" enctype="multipart/form-data" action="{{ route('admin.scorm.store') }}">@csrf<div class="row g-3"><div class="col-md-4"><label class="form-label">Название</label><input class="form-control" name="title" required></div><div class="col-md-4"><label class="form-label">SCORM ZIP</label><input class="form-control" type="file" name="package" accept=".zip" required></div><div class="col-md-2"><label class="form-label">Попыток</label><input class="form-control" type="number" min="1" name="max_attempts"></div><div class="col-md-2"><label class="form-label">Проходной балл</label><input class="form-control" type="number" step="0.01" min="0" max="100" name="pass_score"></div><div class="col-12"><button class="btn btn-dark">Загрузить</button></div></div></form></div></div><div class="table-responsive"><table class="table"><thead><tr><th>Название</th><th>Версия</th><th>Попытки</th><th>Балл</th><th>Результатов</th><th></th></tr></thead><tbody>@foreach($packages as $p)<tr><td>{{ $p->title }}</td><td>{{ $p->version }}</td><td>{{ $p->max_attempts ?: '∞' }}</td><td>{{ $p->pass_score ?? '—' }}</td><td>{{ $p->attempts()->count() }}</td><td class="text-end"><a class="btn btn-sm btn-primary" href="{{ route('scorm.launch',$p) }}">Запустить</a><form method="post" action="{{ route('admin.scorm.destroy',$p) }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Удалить</button></form></td></tr>@endforeach</tbody></table></div>{{ $packages->links() }}@endsection

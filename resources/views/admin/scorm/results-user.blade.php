@extends('admin.layout') @section('title','SCORM — '.$user->name) @section('content')<a href="{{ route('admin.scorm-results.index') }}" class="text-decoration-none">← Результаты</a><h1 class="mt-2">{{ $user->name }}</h1><div class="text-muted mb-3">{{ $user->email }}</div><div class="card admin-card shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Дата</th><th>Пакет</th><th>Курс / урок</th><th>Статус</th><th>Балл</th><th>Время</th></tr></thead><tbody>@forelse($attempts as $a)<tr><td>{{ $a->started_at?->format('d.m.Y H:i') }}</td><td>{{ $a->package?->title }}</td><td>{{ $a->lesson?->course?->title }}<div class="small text-muted">{{ $a->lesson?->title }}</div></td><td><span class="badge {{ in_array($a->status,['passed','completed'])?'text-bg-success':($a->status==='failed'?'text-bg-danger':'text-bg-light') }}">{{ $a->status }}</span></td><td>{{ $a->score_raw ?? '—' }}</td><td>{{ $a->session_time ?: '—' }}</td></tr>@empty<tr><td colspan="6" class="text-muted p-4">Попыток нет.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $attempts->links() }}</div>@endsection

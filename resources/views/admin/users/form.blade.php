@extends('admin.layout') @section('title',$user->exists?'Пользователь':'Новый пользователь') @section('content')<div class="mb-4"><a href="{{ route('admin.users.index') }}" class="text-decoration-none">← Пользователи</a><h1 class="mt-2">{{ $user->exists?'Редактирование пользователя':'Новый пользователь' }}</h1></div><form method="post" action="{{ $user->exists?route('admin.users.update',$user):route('admin.users.store') }}" class="card admin-card shadow-sm"><div class="card-body p-4">@csrf @if($user->exists)@method('PUT')@endif<div class="row g-3"><div class="col-md-6"><label class="form-label">ФИО</label><input name="name" class="form-control" required value="{{ old('name',$user->name) }}"></div><div class="col-md-6"><label class="form-label">Email</label><input type="email" name="email" class="form-control" required value="{{ old('email',$user->email) }}"></div><div class="col-md-6"><label class="form-label">Роль</label><select name="role" class="form-select" required>@foreach(['student'=>'Студент','teacher'=>'Преподаватель','editor'=>'Редактор контента','admin'=>'Администратор'] as $k=>$v)<option value="{{ $k }}" @selected(old('role',$user->role?:'student')===$k)>{{ $v }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Пароль {{ $user->exists?'(оставьте пустым, чтобы не менять)':'' }}</label><input type="password" name="password" class="form-control" {{ $user->exists?'':'required' }}></div><div class="col-12"><label class="form-label">Учебные группы</label><div class="row g-2">@foreach($groups as $g)<div class="col-md-4"><label class="border rounded p-2 d-block"><input type="checkbox" name="group_ids[]" value="{{ $g->id }}" @checked(in_array($g->id,old('group_ids',$user->exists?$user->groups()->pluck('groups.id')->all():[])))> {{ $g->name }}</label></div>@endforeach</div></div></div></div><div class="card-footer bg-white border-0 p-4 pt-0"><button class="btn btn-primary">Сохранить</button>@if($user->exists&&$user->id!==auth()->id())<button form="delete-user" type="button" onclick="if(confirm('Удалить пользователя?'))document.getElementById('delete-user').submit()" class="btn btn-outline-danger ms-2">Удалить</button>@endif</div></form>@if($user->exists&&$user->id!==auth()->id())<form id="delete-user" method="post" action="{{ route('admin.users.destroy',$user) }}">@csrf @method('DELETE')</form>@endif@endsection

@extends('admin.layout') @section('content')<h1>Пользователь</h1><form method="post" action="{{ route('admin.users.update',$user) }}">@csrf @method('PUT')<div class="row g-3"><div class="col-md-6"><label class="form-label">Имя</label><input class="form-control" name="name" required value="{{ old('name',$user->name) }}"></div><div class="col-md-6"><label class="form-label">Email</label><input class="form-control" name="email" required value="{{ old('email',$user->email) }}"></div><div class="col-md-4"><label class="form-label">Роль</label><select name="role" class="form-select">@foreach(['user'=>'Пользователь','student'=>'Студент','teacher'=>'Преподаватель','editor'=>'Редактор','admin'=>'Администратор'] as $k=>$v)<option value="{{ $k }}" @selected(old('role',$user->role)===$k)>{{ $v }}</option>@endforeach</select></div><div class="col-md-8"><label class="form-label">Группы</label><select class="form-select" name="group_ids[]" multiple size="7">@foreach($groups as $g)<option value="{{ $g->id }}" @selected(in_array($g->id,old('group_ids',$user->groups()->pluck('groups.id')->all())))>{{ $g->name }}</option>@endforeach</select></div></div><button class="btn btn-primary mt-4">Сохранить</button></form>@endsection

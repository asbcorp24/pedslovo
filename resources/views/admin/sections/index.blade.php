@extends('admin.layout') @section('title','Разделы') @section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><div class="small-label">Структура портала</div><h1>Разделы и специальности</h1></div><a class="btn btn-primary" href="{{ route('admin.sections.create') }}">Добавить</a></div><div class="alert alert-light border">Порядок можно менять перетаскиванием строк за значок ↕. Изменения сохраняются автоматически.</div><div class="card admin-card shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th></th><th>Название</th><th>Родитель</th><th>Тип</th><th></th></tr></thead><tbody class="js-sortable" data-sort-url="{{ route('admin.sort.update','sections') }}">@foreach($sections as $s)<tr data-sort-id="{{ $s->id }}"><td class="drag-handle">↕</td><td><strong>{{ $s->title }}</strong></td><td>{{ optional($s->parent)->title ?: '—' }}</td><td>{{ $s->type }}</td><td class="text-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.sections.edit',$s) }}">Изменить</a><form method="post" action="{{ route('admin.sections.destroy',$s) }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Удалить?')">Удалить</button></form></td></tr>@endforeach</tbody></table></div></div>@endsection

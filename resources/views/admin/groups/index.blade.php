@extends('admin.layout') @section('title','Учебные группы') @section('content')<div class="d-flex justify-content-between align-items-center"><h1>Учебные группы</h1><a class="btn btn-primary" href="{{ route('admin.groups.create') }}">Добавить группу</a></div><div class="table-responsive mt-3"><table class="table align-middle"><thead><tr><th>Группа</th><th>Куратор</th><th>Слушателей</th><th>Курсов</th><th></th></tr></thead><tbody>@foreach($groups as $g)<tr><td><strong>{{ $g->name }}</strong><div class="text-muted small">{{ $g->code }}</div></td><td>{{ optional($g->curator)->name ?: '—' }}</td><td>{{ $g->users_count }}</td><td>{{ $g->courses_count }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.groups.edit',$g) }}">Изменить</a><form class="d-inline" method="post" action="{{ route('admin.groups.destroy',$g) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Удалить</button></form></td></tr>@endforeach</tbody></table></div>{{ $groups->links() }}@endsection

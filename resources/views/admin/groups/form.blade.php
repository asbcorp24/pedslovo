@extends('admin.layout') @section('content')<h1>{{ $group->exists?'Редактирование группы':'Новая группа' }}</h1><form method="post" action="{{ $group->exists?route('admin.groups.update',$group):route('admin.groups.store') }}">@csrf @if($group->exists)@method('PUT')@endif<div class="row g-3"><div class="col-md-6"><label class="form-label">Название</label><input class="form-control" name="name" required value="{{ old('name',$group->name) }}"></div><div class="col-md-3"><label class="form-label">Код</label><input class="form-control" name="code" value="{{ old('code',$group->code) }}"></div><div class="col-md-3"><label class="form-label">Куратор</label><select class="form-select" name="curator_id"><option value="">—</option>@foreach($users as $u)<option value="{{ $u->id }}" @selected(old('curator_id',$group->curator_id)==$u->id)>{{ $u->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Слушатели</label><select class="form-select" name="user_ids[]" multiple size="12">@foreach($users as $u)<option value="{{ $u->id }}" @selected(in_array($u->id,old('user_ids',$group->exists?$group->users()->pluck('users.id')->all():[])))>{{ $u->name }} — {{ $u->email }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Назначенные курсы</label><select class="form-select" name="course_ids[]" multiple size="12">@foreach($courses as $c)<option value="{{ $c->id }}" @selected(in_array($c->id,old('course_ids',$group->exists?$group->courses()->pluck('courses.id')->all():[])))>{{ $c->title }}</option>@endforeach</select></div><div class="col-12 form-check ms-2"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked(old('is_active',$group->exists?$group->is_active:true))><label class="form-check-label">Активна</label></div></div><button class="btn btn-primary mt-4">Сохранить</button></form>@endsection

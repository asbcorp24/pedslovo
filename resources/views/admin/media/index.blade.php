@extends('admin.layout') @section('title','Медиатека') @section('content')<div class="d-flex justify-content-between align-items-center"><div><div class="small-label">Контент</div><h1>Медиатека</h1></div></div><div class="card admin-card shadow-sm my-3"><div class="card-body"><form method="post" enctype="multipart/form-data" action="{{ route('admin.media.store') }}" class="row g-2">@csrf<div class="col-md-6"><input class="form-control" type="file" name="files[]" multiple required></div><div class="col-md-4"><input class="form-control" name="alt" placeholder="ALT-текст (необязательно)"></div><div class="col-md-2"><button class="btn btn-primary w-100">Загрузить</button></div></form></div></div><form class="mb-3"><div class="input-group" style="max-width:480px"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Поиск по имени файла"><button class="btn btn-dark">Найти</button></div></form><div class="row g-3">@foreach($media as $m)<div class="col-6 col-md-4 col-xl-3"><div class="card admin-card shadow-sm h-100">@if(str_starts_with($m->mime??'','image/'))<img src="{{ $m->url }}" class="card-img-top" style="height:170px;object-fit:cover">@else<div class="p-5 text-center bg-light display-6">📄</div>@endif<div class="card-body"><div class="fw-semibold text-truncate" title="{{ $m->name }}">{{ $m->name }}</div><div class="small text-muted">{{ number_format($m->size/1024,1) }} КБ</div><input class="form-control form-control-sm mt-2" readonly value="{{ $m->url }}" onclick="this.select()"></div><div class="card-footer bg-white border-0"><form method="post" action="{{ route('admin.media.destroy',$m) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Удалить файл?')">Удалить</button></form></div></div></div>@endforeach</div><div class="mt-3">{{ $media->links() }}</div>@endsection

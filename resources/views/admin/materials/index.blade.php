@extends('admin.layout') @section('title','Материалы') @section('content')<div class="d-flex justify-content-between align-items-center mb-4"><h1>Материалы</h1><a class="btn btn-dark" href="{{ route('admin.materials.create') }}">Добавить</a></div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Название</th><th>Тип</th><th>Статус</th><th></th></tr></thead><tbody>@foreach($materials as $m)<tr><td>{{ $m->title }}</td><td>{{ $m->material_type }}</td><td>{{ $m->status }}</td><td class="text-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.materials.edit',$m) }}">Изменить</a><form method="post" action="{{ route('admin.materials.destroy',$m) }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Удалить?')">Удалить</button></form></td></tr>@endforeach</tbody></table></div></div><div class="mt-3">{{ $materials->links() }}</div>@endsection

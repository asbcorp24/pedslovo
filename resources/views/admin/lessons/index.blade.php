@extends('layouts.app')
@section('content')<div class="container py-4"><div class="d-flex justify-content-between align-items-center"><div><a href="{{ route('admin.courses.index') }}" class="text-decoration-none">← Курсы</a><h1 class="mt-2">Уроки: {{ $course->title }}</h1></div><a href="{{ route('admin.courses.lessons.create',$course) }}" class="btn btn-primary">Добавить урок</a></div>@if(session('success'))<div class="alert alert-success mt-3">{{ session('success') }}</div>@endif<div class="table-responsive mt-3"><table class="table align-middle"><thead><tr><th>#</th><th>Название</th><th>Тип</th><th>Источник</th><th>Обязательный</th><th></th></tr></thead><tbody>@forelse($course->lessons as $lesson)<tr><td>{{ $lesson->sort_order }}</td><td>{{ $lesson->title }}</td><td>{{ $lesson->lesson_type }}</td><td>{{ $lesson->material?->title ?? $lesson->scormPackage?->title ?? '—' }}</td><td>{{ $lesson->is_required?'Да':'Нет' }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.courses.lessons.edit',[$course,$lesson]) }}">Изменить</a><form class="d-inline" method="post" action="{{ route('admin.courses.lessons.destroy',[$course,$lesson]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Удалить урок?')">Удалить</button></form></td></tr>@empty<tr><td colspan="6" class="text-muted">Уроков пока нет.</td></tr>@endforelse</tbody></table></div></div>@endsection

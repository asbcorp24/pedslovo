@extends('layouts.app')
@section('content')<div class="container py-4"><div class="d-flex justify-content-between align-items-center"><h1>Учебные курсы</h1><a href="{{ route('admin.courses.create') }}" class="btn btn-primary">Добавить курс</a></div>@if(session('success'))<div class="alert alert-success mt-3">{{ session('success') }}</div>@endif<div class="table-responsive mt-3"><table class="table table-striped align-middle"><thead><tr><th>Название</th><th>Раздел</th><th>Курс обучения</th><th>Статус</th><th></th></tr></thead><tbody>@foreach($courses as $course)<tr><td>{{ $course->title }}</td><td>{{ optional($course->section)->title }}</td><td>{{ $course->study_year ? $course->study_year.' курс' : '—' }}</td><td>{{ $course->is_active?'Активен':'Скрыт' }}</td><td class="text-end"><a href="{{ route('admin.courses.edit',$course) }}" class="btn btn-sm btn-outline-primary">Изменить</a><form class="d-inline" method="post" action="{{ route('admin.courses.destroy',$course) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Удалить курс?')">Удалить</button></form></td></tr>@endforeach</tbody></table></div>{{ $courses->links() }}</div>@endsection

@extends('layouts.app')
@section('title',$section->title)
@section('content')<div class="container py-5"><a href="{{ route('home') }}" class="text-decoration-none">← На главную</a><h1 class="display-6 fw-bold mt-3">{{ $section->title }}</h1>@if($section->description)<p class="lead text-muted">{{ $section->description }}</p>@endif@if($section->children->count())<div class="row g-3 mt-3">@foreach($section->children as $child)<div class="col-md-6 col-lg-4"><a class="text-decoration-none text-dark" href="{{ route('section.show',$child) }}"><div class="card section-card shadow-sm"><div class="card-body p-4"><div class="small text-uppercase text-muted">{{ $child->type }}</div><h2 class="h5 mt-2 mb-0">{{ $child->title }}</h2></div></div></a></div>@endforeach</div>@endif@if($section->materials->count())<h2 class="h3 mt-5">Материалы</h2><div class="list-group shadow-sm">@foreach($section->materials as $material)<a href="{{ route('material.show',$material) }}" class="list-group-item list-group-item-action p-3"><strong>{{ $material->title }}</strong><div class="small text-muted">{{ $material->annotation }}</div></a>@endforeach</div>@endif</div>@endsection
